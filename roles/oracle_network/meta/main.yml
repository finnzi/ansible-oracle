---
galaxy_info:
  author: ansible-oracle
  description: Per-instance listener config bound to a dedicated VIP, with static SID_LIST
  license: MIT
  min_ansible_version: "2.16"
  platforms:
    - name: EL
      versions: ["9"]
dependencies:
  - role: oracle_storage
