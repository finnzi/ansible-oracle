---
# tasks/register-instance.yml — srvctl add + start one instance.
# `inst` is in scope (one element of oracle_instances).

- name: Resolve current ORACLE_HOME for Restart registration
  ansible.builtin.set_fact:
    _restart_home_path: >-
      {{ inst.oracle_base }}/{{ ((inst.db_homes | selectattr('current', 'equalto', true) | list | first) | default(inst.db_homes[0])).suffix }}

- name: Is this DB already registered with Restart?
  ansible.builtin.shell: |
    set -o pipefail
    export ORACLE_HOME={{ oracle_gi_home }}
    "$ORACLE_HOME/bin/srvctl" config database -d {{ inst.name }} 2>&1 || echo NOT_REGISTERED
  args: { executable: /bin/bash }
  become: true
  become_user: "{{ oracle_user }}"
  register: _srvctl_cfg
  changed_when: false
  failed_when: false

- name: Srvctl add database (idempotent)
  ansible.builtin.shell: |
    set -o pipefail
    export ORACLE_HOME={{ oracle_gi_home }}
    "$ORACLE_HOME/bin/srvctl" add database \
      -db {{ inst.name }} \
      -oraclehome {{ _restart_home_path }} \
      -spfile {{ inst.dirs.data }}/{{ inst.name }}/spfile{{ inst.name }}.ora \
      -role PRIMARY \
      -dbname {{ inst.name }} \
      -instance {{ inst.name }} \
      -startoption OPEN \
      -stopoption IMMEDIATE
  args: { executable: /bin/bash }
  become: true
  become_user: "{{ oracle_user }}"
  when: "'NOT_REGISTERED' in _srvctl_cfg.stdout or _srvctl_cfg.rc != 0"
  register: _srvctl_add
  changed_when: _srvctl_add.rc == 0
  failed_when: >-
    _srvctl_add.rc != 0 and 'already exists' not in (_srvctl_add.stdout | default(''))

# If a spfile isn't at the declared path yet, point Restart at the default in
# $ORACLE_HOME/dbs. We try the canonical path; on failure we re-add with the
# default. Keep this defensive — the slice tolerates it.
- name: Fallback — re-add using default spfile location if canonical missing
  ansible.builtin.shell: |
    set -o pipefail
    export ORACLE_HOME={{ oracle_gi_home }}
    "$ORACLE_HOME/bin/srvctl" modify database \
      -db {{ inst.name }} \
      -spfile {{ _restart_home_path }}/dbs/spfile{{ inst.name }}.ora
  args: { executable: /bin/bash }
  become: true
  become_user: "{{ oracle_user }}"
  register: _srvctl_mod_spfile
  changed_when: false
  failed_when: false

- name: Srvctl add listener for this instance (idempotent)
  ansible.builtin.shell: |
    set -o pipefail
    export ORACLE_HOME={{ oracle_gi_home }}
    "$ORACLE_HOME/bin/srvctl" add listener \
      -listener LISTENER_{{ inst.name | upper }} \
      -oraclehome {{ _restart_home_path }} \
      -endpoints "TCP:{{ inst.listener_port | default(1521) }}"
  args: { executable: /bin/bash }
  become: true
  become_user: "{{ oracle_user }}"
  register: _srvctl_add_lsnr
  changed_when: _srvctl_add_lsnr.rc == 0
  failed_when: >-
    _srvctl_add_lsnr.rc != 0 and 'already exists' not in (_srvctl_add_lsnr.stdout | default(''))

- name: Srvctl enable database + listener
  ansible.builtin.shell: |
    set -o pipefail
    export ORACLE_HOME={{ oracle_gi_home }}

    enable_resource() {
      label="$1"
      shift
      enable_output="$("$ORACLE_HOME/bin/srvctl" enable "$@" 2>&1)"
      enable_rc=$?
      printf '%s\n' "$enable_output"
      if [ "$enable_rc" -ne 0 ] &&
         ! printf '%s\n' "$enable_output" | grep -Eiq 'already enabled|PRCR-1002'; then
        return "$enable_rc"
      fi
      if [ "$enable_rc" -eq 0 ]; then
        printf '%s_ENABLED\n' "$label"
      else
        printf '%s_ALREADY_ENABLED\n' "$label"
      fi
    }

    enable_resource DATABASE database -d {{ inst.name }}
    database_rc=$?
    enable_resource LISTENER listener -listener LISTENER_{{ inst.name | upper }}
    listener_rc=$?

    if [ "$database_rc" -ne 0 ]; then
      exit "$database_rc"
    fi
    exit "$listener_rc"
  args: { executable: /bin/bash }
  become: true
  become_user: "{{ oracle_user }}"
  register: _srvctl_enable
  changed_when: >-
    'DATABASE_ENABLED' in (_srvctl_enable.stdout | default('')) or
    'LISTENER_ENABLED' in (_srvctl_enable.stdout | default(''))
  failed_when: _srvctl_enable.rc != 0

- name: Srvctl start database + listener (if not already running)
  ansible.builtin.shell: |
    set -o pipefail
    export ORACLE_HOME={{ oracle_gi_home }}

    start_resource() {
      label="$1"
      shift
      status_output="$("$ORACLE_HOME/bin/srvctl" status "$@" 2>&1)" || true
      printf '%s\n' "$status_output"
      if printf '%s\n' "$status_output" | grep -Eq 'is running|is already running|running on node'; then
        printf '%s_ALREADY_RUNNING\n' "$label"
        return 0
      fi

      start_output="$("$ORACLE_HOME/bin/srvctl" start "$@" 2>&1)"
      start_rc=$?
      printf '%s\n' "$start_output"
      if [ "$start_rc" -ne 0 ] &&
         ! printf '%s\n' "$start_output" | grep -Eiq 'already running|PRCR-1079'; then
        return "$start_rc"
      fi
      printf '%s_STARTED\n' "$label"
    }

    start_resource LISTENER listener -listener LISTENER_{{ inst.name | upper }}
    listener_rc=$?
    start_resource DATABASE database -d {{ inst.name }}
    database_rc=$?

    if [ "$listener_rc" -ne 0 ]; then
      exit "$listener_rc"
    fi
    exit "$database_rc"
  args: { executable: /bin/bash }
  become: true
  become_user: "{{ oracle_user }}"
  register: _srvctl_start
  changed_when: >-
    'LISTENER_STARTED' in (_srvctl_start.stdout | default('')) or
    'DATABASE_STARTED' in (_srvctl_start.stdout | default(''))
  failed_when: _srvctl_start.rc != 0
