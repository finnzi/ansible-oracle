---
# playbooks/07-patch.yml — Oracle home patching.
# Detection (standby-first eligibility) is real and runs against the staged
# RU zip; the apply itself is scaffolded.

- name: Analyse patch for standby-first eligibility — scaffolded apply
  hosts: oracle_db_hosts
  become: true
  roles:
    - role: oracle_patch
