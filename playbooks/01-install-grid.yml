---
# playbooks/01-install-grid.yml — install Oracle Restart (Grid standalone).
# SCAFFOLDED in this slice: the role verifies the installer is staged and
# reports readiness; it does not install (set oracle_gi_install_enabled=true
# once implemented). oracle_restart_manage then degrades gracefully.

- name: Install Oracle Grid Infrastructure (Restart) — scaffolded
  hosts: oracle_db_hosts
  become: true
  roles:
    - role: oracle_gi_install
